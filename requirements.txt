# Core dependency (v2 required)
pydantic>=2.0.0

# Optional LLM providers (install only what you use)
openai>=1.0.0
anthropic>=0.20.0

# Optional: embedding-based similarity (used when --no-embeddings is NOT set)
numpy>=1.21.0
